import { makeStyles } from '@material-ui/core';

const usePostPageStyles = makeStyles((theme) => ({
  replyWrapper: {
    margin: '20px 0 0 0',
    borderTop: `1px solid #38444D`,
    borderBottom: `1px solid #38444D`,
    padding: '15px 0',
  },
}));

export default usePostPageStyles;
